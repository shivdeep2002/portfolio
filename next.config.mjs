// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.icons8.com"], // Add the domain from which you're loading images
  },
};

export default nextConfig;
